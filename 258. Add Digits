public class Solution {
    public int addDigits(int num) {
        int m = (num-9)%9;
        if ( num <= 9 )
            return num;
        else if (m==0)
            return 9;
        else
            return m;
    }
}
